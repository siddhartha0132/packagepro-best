/** Turns a free-text trip request (any Indian language) into catalogue codes; refuses to invent a destination. */
export function tripParserPrompt(input: { today: string; origins: string; destinations: string }) {
  return `Extract a travel request into JSON. Only return a JSON object, no markdown, no prose. Keys: isTripRequest (boolean), originCode, destinationCode, durationDays, departDate (YYYY-MM-DD), returnDate, hotelTier (budget|boutique|luxury), transportMode (flight|train|cab). Match origin and destination to these exact catalogue codes. The text may be in any Indian language. Set isTripRequest true ONLY when the traveller explicitly names a destination city (in any language or script). Interests, a vibe, a theme (beach, honeymoon, trekking) or a budget without a named city is NOT a trip request — set isTripRequest false. Never invent an origin or destination that was not written. Resolve relative dates against today ${input.today}. Origins: ${input.origins}. Destinations: ${input.destinations}.`;
}
